%h1 Listing sections

%table
  %tr
    %th Name
    %th
    %th
    %th

  - @sections.each do |section|
    %tr
      %td= section.name
      %td= link_to 'Show', admin_section_path(section)
      %td= link_to 'Edit', edit_admin_section_path(section)
      %td= link_to 'Destroy', admin_section_path(section), :method => :delete, :data => { :confirm => 'Are you sure?' }
%br


= link_to 'New Section', new_admin_section_path
